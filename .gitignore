result
*.swp
*~

# age private keys — NEVER commit
*.key
personal-age.key
/var/lib/sops-nix/
.keys/

# local plaintext accidents (real secrets/arr.yaml is sops-ENCRYPTED and IS committed)
secrets/*.plain
secrets/*.dec.yaml
